vbetool: vbetool
vbetool: 
vbetool: vbetool uses lrmi in order to run code from the video
vbetool: BIOS. Currently, it is able to alter DPMS states, save/restore
vbetool: video card state, and attempt to initialize the video card from
vbetool: scratch. It exists primarily in order to increase the chances of
vbetool: successfully recovering video state after an ACPI S3 suspend.
vbetool: 
vbetool: 
vbetool: 
vbetool: 
vbetool: 
vbetool: 
