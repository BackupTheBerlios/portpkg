when: A Geek Calendar
when:
when: When is an extremely simple personal calendar program, aimed at
when: the Unix geek who wants something minimalistic. It can keep track
when: of things you need to do on particular dates. The configuration is
when: done through a simple plain text file and it just needs perl to run.
when:
when:
when:
when:
when:
