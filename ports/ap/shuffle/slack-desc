shuffle: shuffle (randomizer)
shuffle:
shuffle: Randomize file(s) / stdin...randomly shuffling x inputs to
shuffle: provide n lines of output. If randomizing a file, file contents
shuffle: are replaced. Initially developed to easily facilitate playing
shuffle: media files at random.
shuffle:
shuffle:
shuffle:
shuffle:
shuffle:
