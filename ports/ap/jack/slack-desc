jack: Jack Audio Connection Kit
jack:
jack: JACK is a low-latency audio server. It can connect a number of
jack: different applications to an audio device, as well as allowing them
jack: to share audio between themselves. Its clients can run in their own
jack: processes (i.e., as normal applications), or can they can run within
jack: the JACK server (as a "plugin").
jack:
jack:
jack:
jack:
