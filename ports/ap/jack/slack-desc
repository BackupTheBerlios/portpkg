jack: jack-audio-connection-kit
jack:
jack: JACK is a low-latency audio server, written for POSIX conformant
jack: operating systems such as GNU/Linux and Apple's OS X. It can connect
jack: a number of different applications to an audio device, as well as
jack: allowing them to share audio between themselves. Its clients can
jack: run in their own processes (ie. as normal applications), or can they
jack: can run within the JACK server (ie. as a "plugin").
jack:
