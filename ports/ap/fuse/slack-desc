fuse: Filesystem In Userspace
fuse:
fuse: FUSE is a simple interface for userspace programs to export a
fuse: virtual filesystem to the Linux kernel. It also aims to provide a
fuse: secure method for non privileged users to create and mount their
fuse: own filesystem implementations.
fuse:
fuse:
fuse:
fuse:
fuse:
