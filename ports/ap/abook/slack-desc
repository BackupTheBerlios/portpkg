abook: Address Book
abook:
abook: Abook is a text-based addressbook program designed to use with the
abook: Mutt MUA. It uses a ncurses user interface.
abook:
abook:
abook:
abook:
abook:
abook:
abook:
