2005-05-03 13:55  topf

	* SlackBuild, classification.config.new, footprint,
	  gen-msg.map.new, md5sums, reference.config.new, rules.tar.bz2,
	  sid-msg.map.new, sid.new, snort.conf.new, sources: Moved
	  open/snort to n/.

2005-04-28 15:38  topf

	* SlackBuild, classification.config.new, doinst.sh, footprint,
	  gen-msg.map.new, generators.new, reference.config.new, requires,
	  rules.tar.bz2, sid-msg.map.new, sid.new, slack-desc,
	  snort.conf.new, sources, threshold.conf.new, unicode.map.new:
	  Moved open/snort to n/. (stb)

