2005-08-22 08:07  hnaz

	* SlackBuild, doinst.sh, footprint, slack-desc: Handle config files
	  like we always do (.new and config()).  Cleaned up SlackBuild and
	  slack-desc.

2005-07-24 12:21  topf

	* SlackBuild, doinst.sh, footprint, md5sums, requires, slack-desc,
	  sources, squid.init, squid.logrotate: Moved
	  open/squid-2.5.STABLE9-x86_64-1stb to n/. (stb)

