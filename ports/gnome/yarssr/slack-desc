yarssr: yarssr (Yet Another RSS Reader for GNOME)
yarssr:
yarssr: Yet Another RSS Reader is an RSS aggregator and reader that
yarssr: displays its results in the GNOME or KDE system tray
yarssr: (notification area). To view the contents of the feed just
yarssr: click the menu-item and it will launch in your favorite
yarssr: browser. It is written in Perl and uses gtk2-perl for it's
yarssr: interface. Yarssr is still early in development, but is
yarssr: entirely usable.
yarssr:
