scrollkeeper: scrollkeeper
scrollkeeper:
scrollkeeper: ScrollKeeper is a cataloging system for documentation. It
scrollkeeper: manages documentation metadata as specified by the Open
scrollkeeper: Source Metadata Framework (OMF) and provides a simple API to
scrollkeeper: allow help browsers to find, sort, and search the document
scrollkeeper: catalog. It will also be able to communicate with catalog
scrollkeeper: servers on the Net to search for documents which are not on
scrollkeeper: the local system.
scrollkeeper:
scrollkeeper:
