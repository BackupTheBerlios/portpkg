hal: hal (hardware abstraction layer)
hal: 
hal: You must add a user and a group "haldaemon" to your system
hal: to use hal:
hal:   # groupadd haldaemon
hal:   # useradd haldaemon
hal: Start it with:
hal:   # /etc/rc.d/rc.hald start
hal:
