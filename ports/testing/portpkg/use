#
# USE these ports as required dependencies when they are listed as optional
#

# Example: To build any GNOME dependencies for packages that can be built with
#   or without GNOME libraries, uncomment this line:
#
#gnome/
