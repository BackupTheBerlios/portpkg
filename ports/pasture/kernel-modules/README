
This SlackBuild may need some tweaking, however, it's working and provides
kernel modules according to the config file in this directory.
Possible improvements (SlackBuild): make content of doinst.sh automatically reflect 
the kernel version ( config-$VERSION.new recognition (sed?))