NAME=gstreamer
VERSION=0.8.7
ARCH=i486
BUILD=3slack

checkinstall -S -y \
  --pkgname=$NAME \
  --pkgversion=$VERSION \
  --pkgarch=$ARCH \
  --pkgrelease=$BUILD \
  --pakdir=/tmp \
  --strip=yes \
  --stripso=yes \
  --gzman=yes \
  --backup=no \
  "sh gstreamer.build" || exit 1
