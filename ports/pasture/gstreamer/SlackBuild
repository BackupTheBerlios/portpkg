# this is a wrapper around the original build-script from
# Slackware-current, since on current-systems this is already
# installed, portpkg won't use it

NAME=gstreamer
VERSION=0.8.7
ARCH=i486
BUILD=1

# we need to remove any other package before checkinstall
# runs a installpkg on it

removepkg $NAME

checkinstall -S -y \
  --pkgname=$NAME \
  --pkgversion=$VERSION \
  --pkgarch=$ARCH \
  --pkgrelease=$BUILD \
  --pakdir=/tmp \
  --strip=yes \
  --stripso=yes \
  --gzman=yes \
  --backup=no \
  "sh gstreamer.build" || exit 1
