2005-12-05  topf  <topf@users.berlios.de>

  * SlackBuild, doinst.sh, firefox.desktop, footprint, info, md5sums, mozilla-firefox.desktop, requires, slack-desc, sources:
  version update (lem)

2005-07-24  topf  <topf@users.berlios.de>

  * SlackBuild, doinst.sh, firefox.desktop, footprint, info, md5sums, mozilla-firefox.desktop, requires, slack-desc, sources:
  Moved to pasture/.

2005-07-14  topf  <topf@users.berlios.de>

  * SlackBuild, doinst.sh, firefox.desktop, footprint, info, md5sums, mozilla-firefox.desktop, requires, slack-desc, sources:
  version update (lem)

2005-05-16  topf  <topf@users.berlios.de>

  * SlackBuild, doinst.sh, firefox.desktop, footprint, info, md5sums, mozilla-firefox.desktop, requires, slack-desc, sources:
  Updated version, added line 62,63,64 in SlackBuild (patch moz_libdir,MRE_HOME path) (sjn)

2005-05-14  topf  <topf@users.berlios.de>

  * SlackBuild, doinst.sh, firefox.desktop, footprint, info, md5sums, mozilla-firefox.desktop, requires, slack-desc, sources:
  Updated version, added line 62,63,64 in SlackBuild (patch moz_libdir,MRE_HOME path) (cronjob)

