NAME=gst-plugins
VERSION=0.8.5
ARCH=i486
BUILD=3slack

checkinstall -S -y \
  --pkgname=$NAME \
  --pkgversion=$VERSION \
  --pkgarch=$ARCH \
  --pkgrelease=$BUILD \
  --pakdir=/tmp \
  --strip=yes \
  --stripso=yes \
  --gzman=yes \
  --backup=no \
  "sh gst-plugins.build" || exit 1
